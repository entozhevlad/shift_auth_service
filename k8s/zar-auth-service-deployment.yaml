apiVersion: apps/v1
kind: Deployment
metadata:
  name: zar-auth-service
  namespace: test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: zar-auth-service
  template:
    metadata:
      labels:
        app: zar-auth-service
    spec:
      containers:
      - name: zar-auth-service
        image: your-docker-registry/zar-auth-service:latest
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        env:
        - name: DATABASE_URL
          value: "postgresql+asyncpg://username:password@postgres-service:5432/vzarubin"
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: zar-app-config
              key: KAFKA_BOOTSTRAP_SERVERS
        volumeMounts:
        - name: photos-storage
          mountPath: /app/photos
      volumes:
      - name: photos-storage
        persistentVolumeClaim:
          claimName: zar-photos-pvc
